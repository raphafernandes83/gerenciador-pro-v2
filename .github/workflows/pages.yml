name: GitHub Pages legado desativado

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Informar destino de deploy
        run: |
          echo "GitHub Pages legado nao publica mais automaticamente."
          echo "O ultimo artefato publicado permanece online ate a migracao final para Cloudflare."
          echo "Novas publicacoes devem seguir o fluxo controlado da Cloudflare."
